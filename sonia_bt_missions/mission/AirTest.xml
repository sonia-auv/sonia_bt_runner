<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4">
  <BehaviorTree ID="Air01MoveToObject">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="5"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <MoveToObject Offset="0"
                    Trajectory="{Trajectory}"
                    Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air02UninitializedTrajectory">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="2"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <MoveToObject Offset="0"
                    Trajectory="{Trajectory}"
                    Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air03TimerAiFilter">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="5"
                Max_frame_before_failing="0"
                Two_objects_possible="0"
                Min_detections_before_success="2"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <MoveToObject Offset="0"
                    Trajectory="{Trajectory}"
                    Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air04MinDetectionFault">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="1"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <MoveToObject Offset="0"
                    Trajectory="{Trajectory}"
                    Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air05AimingPortSide">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="2"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <TorpedoAiming Trajectory="{Trajectory}"
                     TargetOffset="0"
                     LaunchingSide="port_side"
                     Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air06AimingStarboard">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="2"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <TorpedoAiming Trajectory="{Trajectory}"
                     TargetOffset="0"
                     LaunchingSide="starboard"
                     Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air07AimingSub">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="2"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <TorpedoAiming Trajectory="{Trajectory}"
                     TargetOffset="0"
                     LaunchingSide="Sub"
                     Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air08AimingSubOffset2">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="2"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <TorpedoAiming Trajectory="{Trajectory}"
                     TargetOffset="2"
                     LaunchingSide="Sub"
                     Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air09Drifting">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="2"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="bin-inner"
                Camera="torpedo-poster"/>
      <ObjectDrifting Trajectory="{Trajectory}"
                      Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air10IdenticalParameter">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="5"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <MoveToObject Offset="0"
                    Trajectory="{Trajectory}"
                    Detection="{AiDetection}"/>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="5"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <MoveToObject Offset="0"
                    Trajectory="{Trajectory}"
                    Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="Air11FaceTheObject">
    <Sequence>
      <RetryUntilSuccessful num_attempts="-1">
        <MissionSwitchStatus/>
      </RetryUntilSuccessful>
      <InitializeTrajectory trajectory="{Trajectory}"/>
      <AiActivation Model="0"
                    Bottom="false"
                    Front="true"/>
      <AiFilter Detected_object="{AiDetection}"
                Max_time_before_failing_sec="0.000000"
                Max_frame_before_failing="300"
                Two_objects_possible="0"
                Min_detections_before_success="5"
                Confidence="0.3000"
                Max_depth="25"
                Object_class="torpedo-poster"
                Camera="1"/>
      <FaceTheObject Trajectory="{Trajectory}"
                     Detection="{AiDetection}"/>
    </Sequence>
  </BehaviorTree>

  <!-- Description of Node Models (used by Groot) -->
  <TreeNodesModel>
    <Action ID="AiActivation">
      <input_port name="Model"
                  default="0"
                  type="int">model choice, default = 0</input_port>
      <input_port name="Bottom"
                  default="false"
                  type="bool">true: ON, talse: OFF</input_port>
      <input_port name="Front"
                  default="false"
                  type="bool">true: ON, talse: OFF</input_port>
    </Action>
    <Action ID="AiFilter">
      <output_port name="Detected_object"
                   type="AiDetection"/>
      <input_port name="Max_time_before_failing_sec"
                  default="0.000000"
                  type="float">Maximum allowed time in ms before failing the object research</input_port>
      <input_port name="Max_frame_before_failing"
                  default="0"
                  type="int">Maximum allowed frames before failing the object research</input_port>
      <input_port name="Two_objects_possible"
                  default="0"
                  type="int">0: Only one object can be detected, 1: Two or more object of the same class can be detected</input_port>
      <input_port name="Min_detections_before_success"
                  default="2"
                  type="int">Minumum number of frames with at least one detection before sending results. Need to be heigher than 2</input_port>
      <input_port name="Confidence"
                  default="0.600000"
                  type="float">Ai confidence</input_port>
      <input_port name="Max_depth"
                  default="25.000000"
                  type="float">Maximum allowed depth</input_port>
      <input_port name="Object_class"
                  type="std::string">Searched object</input_port>
      <input_port name="Camera"
                  default="1"
                  type="int">1: Front, 0: Bottom</input_port>
    </Action>
    <Action ID="FaceTheObject">
      <inout_port name="Trajectory"
                  type="Trajectory"/>
      <input_port name="Detection"
                  type="AiDetection"/>
    </Action>
    <Action ID="InitializeTrajectory">
      <output_port name="trajectory"
                   type="Trajectory"/>
    </Action>
    <Condition ID="MissionSwitchStatus"/>
    <Action ID="MoveToObject">
      <input_port name="Offset"
                  type="float"/>
      <inout_port name="Trajectory"
                  type="Trajectory"/>
      <input_port name="Detection"
                  type="AiDetection"/>
    </Action>
    <Action ID="ObjectDrifting">
      <inout_port name="Trajectory"
                  type="Trajectory"/>
      <input_port name="Detection"
                  type="AiDetection"/>
    </Action>
    <Action ID="TorpedoAiming">
      <inout_port name="Trajectory"
                  type="Trajectory"/>
      <input_port name="TargetOffset"
                  type="float"/>
      <input_port name="LaunchingSide"
                  type="std::string"/>
      <input_port name="Detection"
                  type="AiDetection"/>
    </Action>
  </TreeNodesModel>

</root>
